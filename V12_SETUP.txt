SKTech Exam Portal — V12 Core Fix

1) Deploy this ZIP to the SAME Candidate and Admin Vercel projects as the new deployment.
2) In Supabase SQL Editor run: supabase_schema_v12.sql once.
3) Do not put SUPABASE_SERVICE_ROLE_KEY or OpenAI secret in VITE_* variables.
4) Question TXT is the primary import format. Supported examples include:
   Q1. [Banking Awareness] question
   A) option
   B) option
   C) option
   D) option
   उत्तर: B) ...
   विवरण: ...
   and
   1. [Prelims | Inequality] question
   A. ...
5) V12 blocks incomplete records from publishing and quarantines old malformed approved records.
6) Admin import uses a protected Supabase RPC and performs exact normalized duplicate checks.
7) Exam creation uses a protected RPC and candidate mocks only read approved valid questions.
8) First test with a small TXT (20–50 questions), verify Question Bank and Mock, then upload the large bank.
